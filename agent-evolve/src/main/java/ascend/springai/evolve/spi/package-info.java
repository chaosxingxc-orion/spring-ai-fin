/**
 * agent-evolve SPI — placeholder (skeleton).
 *
 * <p>Reserved for the Java-side evolution-plane adapter SPI surface once
 * the Python ML pipeline integration lands. Today the package exists only
 * so {@code module-metadata.yaml} can declare
 * {@code spi_packages: [ascend.springai.evolve.spi]} and pass gate Rule 36
 * ({@code domain_module_has_spi_package}).
 *
 * <p>Authority: ADR-0075, Layer-0 principle P-I, Rule 47.
 */
package ascend.springai.evolve.spi;
